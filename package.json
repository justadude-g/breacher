{
  "name": "breacher",
  "version": "1.0.0",
  "private": true,
  "description": "Breacher force builder and card designer \u2014 static site, no build step.",
  "scripts": {
    "test": "node test/verify1.js",
    "serve": "python3 -m http.server 8000",
    "setup": "npm install && PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT=180000 npx playwright install --only-shell chromium",
    "setup:retry": "PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT=300000 npx playwright install --only-shell chromium"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
